#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../enzyme-adapter-react-15/node_modules/loose-envify/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../enzyme-adapter-react-15/node_modules/loose-envify/cli.js" "$@"
  ret=$?
fi
exit $ret
